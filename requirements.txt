# Core data
numpy>=1.24
pandas>=2.0
pyarrow>=13.0          # parquet I/O in the regression pipeline

# ML
scikit-learn>=1.3      # MinMaxScaler, PLSRegression, train_test_split, metrics
tensorflow>=2.13       # includes keras (Sequential, Conv2D, etc.) used in train.py

# Visualisation
matplotlib>=3.7

# Regression pipeline — distributed query layer
pyspark>=3.4
# pyspark-llap                # Hive Warehouse Connector; install separately from your
#                             # cluster distribution (not on PyPI). Required only when
#                             # SparkSession.use() / Hive queries are called.

# Dashboard
flask>=3.0
