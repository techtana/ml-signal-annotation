# Core data
numpy>=1.24
pandas>=2.0

# ML
scikit-learn>=1.3      # MinMaxScaler, train_test_split
tensorflow>=2.13       # includes keras (Sequential, Conv2D, etc.)
# tensorflow-directml-plugin  # Windows-only: GPU via DirectML (works on NVIDIA + AMD)
#                             # install separately: pip install tensorflow-directml-plugin

# Web app
flask>=3.0
